\section{Benchmarking} \label{sec:9_benchmarking}
\captionsetup[table]{justification=centering,singlelinecheck=false}
\captionsetup[figure]{justification=centering,singlelinecheck=false}
\newcommand{\thead}[1]{\shortstack[c]{\bfseries #1}}

En esta sección se presenta el análisis de los resultados obtenidos a través del módulo
de \textit{benchmarking}. Se detalla el objetivo de dicho análisis, la metodología seguida para
su ejecución, el entorno experimental utilizado y las comparativas realizadas.

\subsection{Objetivo} \label{sec:9_benchmarking_objective}
El objetivo de este análisis es la comparación del rendimiento de distintos
\textit{pipelines} de procesado de imágenes bajo diversas condiciones de ejecución.
Para ello, se tendrá en cuenta tanto la influencia del hardware como las diferentes configuraciones de procesamiento,
con el fin de evaluar su eficiencia en escenarios de procesamiento en tiempo real.
\noindent En particular, el análisis busca responder a las siguientes preguntas:

\begin{enumerate}[noitemsep]
	\item ¿Qué ganancia aporta la ejecución de \textit{pipelines} de procesado en GPU respecto a CPU?
	\item ¿Cómo varía el rendimiento con la resolución de imagen y con el tipo numérico empleado?
	\item ¿Qué impacto tiene el aumento de la complejidad del \textit{pipeline} de procesado?
	\item ¿Qué diferencias de coste temporal presentan los módulos de \textit{Debayer} implementados?
	\item ¿Cuál es el rendimiento en escenarios de uso realistas?
\end{enumerate}

\subsection{Metodología} \label{sec:9_benchmarking_methodology}
Los resultados analizados proceden de los artefactos JSON generados por el módulo
de \textit{benchmarking}, ejecutado sobre tres equipos
independientes y las siguientes definiciones de \textit{pipelines} de procesado:

\begin{itemize}[noitemsep]
	\item \texttt{standard\_run}: \textit{pipeline} simple de complejidad reducida, consta de las siguientes operaciones:
	      \begin{enumerate}[noitemsep]
		      \item \textit{Debayer5x5()}
		      \item \textit{SigmoidContrast(gain=2.25, cutoff=0.25)}
		      \item \textit{RealToRGB8()}
	      \end{enumerate}
	\item \texttt{complex\_run}: \textit{pipeline} complejo con más operaciones, consta de:
	      \begin{enumerate}[noitemsep]
		      \item \textit{Debayer5x5()}
		      \item \textit{MinMaxNormalization()}
		      \item \textit{GammaAdjustment(c=1.65, gamma=0.8)}
		      \item \textit{UnsharpMasking(kernel\_size=9, sigma=3.0)}
		      \item \textit{RealToRGB8()}

	      \end{enumerate}
	\item \texttt{debayer2x2\_run}, \texttt{debayer3x3\_run}, \texttt{debayer5x5\_run},
	      \texttt{debayerSplit\_run}: perfiles dedicados para comparar las variantes de \textit{Debayer} implementadas.
\end{itemize}

\indent{Las ejecuciones se han llevado a cabo sobre imágenes sintéticas considerando todas las combinaciones posibles de los siguientes factores:}

\begin{itemize}[noitemsep]
	\item \textbf{Resolución de imagen}: \texttt{256x256}, \texttt{512x512}, \texttt{1024x1024} y \texttt{2048x2048}.
	\item \textbf{Dispositivo de ejecución}: \texttt{CPU} y \texttt{GPU}.
	\item \textbf{Tipo numérico de datos}: \texttt{float16}, \texttt{float32} y \texttt{float64}.
\end{itemize}

\newpage

\subsection{Entorno experimental} \label{subsec:9_benchmarking_environment}
Los escenarios de evaluación se han ejecutado sobre tres plataformas hardware
independientes, seleccionadas con el objetivo de cubrir un espectro amplio de
contextos de uso. Cada una de ellas representa un perfil distinto de
despliegue. A continuación se describen las características técnicas de cada equipo y el
escenario de uso que representan.

\subsubsection{Equipo 1. Estación de trabajo de alto rendimiento} \label{subsubsec:equipo1}

El Equipo 1 corresponde a una estación de trabajo de
escritorio, representativa de un entorno de desarrollo, experimentación y
validación de algoritmos. Su especificación de hardware se
detalla en la \autoref{tab:equipo1_specs}.

\begin{table}[h!]
	\centering
	\renewcommand{\arraystretch}{1.25}
	\setlength{\tabcolsep}{10pt}
	\begin{tabularx}{0.85\linewidth}{|>{\hsize=0.55\hsize\centering\arraybackslash}X|>{\hsize=0.85\hsize\centering\arraybackslash}X|>{\hsize=0.6\hsize\centering\arraybackslash}X|}
		\hline
		\rowcolor[gray]{0.90}
		\thead{Componente} & \thead{Modelo}         & \thead{Especificaciones}                                                                \\
		\hline
		\rowcolor{white}
		CPU                 & AMD Ryzen 9 9950X3D     & 16 núcleos / 32 hilos, arquitectura x86\_64, 192 MB caché L3 (64 MB + 128 MB 3D V-Cache) \\
		\hline
		\rowcolor{white}
		GPU                 & NVIDIA GeForce RTX 5090 & Arquitectura Blackwell, 32 GB VRAM                                                       \\
		\hline
		\rowcolor{white}
		Memoria RAM         & DDR5                    & 128 GB                                                                                   \\
		\hline
	\end{tabularx}
	\caption{Especificaciones técnicas del Equipo 1}
	\label{tab:equipo1_specs}
\end{table}

Este equipo se emplea como referencia de rendimiento máximo dentro del estudio. Su combinación de componentes lo sitúa en el
extremo superior del espectro de capacidades disponibles para el desarrollo de algoritmos de visión por computador.
En el contexto del \textit{benchmarking}, permite establecer la cota superior de rendimiento alcanzable para
cada \textit{pipeline} y analizar la escalabilidad de las implementaciones en condiciones donde los recursos
hardware no actúan como factor limitante.

\newpage

\subsubsection{Equipo 2. Sistema embebido de gama alta} \label{subsubsec:equipo2}

El Equipo 2 corresponde a una plataforma de desarrollo NVIDIA Jetson AGX Orin,
un sistema embebido diseñado para cargas de trabajo
de inteligencia artificial y procesado de señales en entornos con restricciones
de energía, espacio y disipación térmica. Sus características se recogen en la
\autoref{tab:equipo2_specs}.

\begin{table}[h!]
	\centering
	\renewcommand{\arraystretch}{1.25}
	\setlength{\tabcolsep}{10pt}
	\begin{tabularx}{0.85\linewidth}{|>{\hsize=0.55\hsize\centering\arraybackslash}X|>{\hsize=0.85\hsize\centering\arraybackslash}X|>{\hsize=0.6\hsize\centering\arraybackslash}X|}
		\hline
		\rowcolor[gray]{0.90}
		\thead{Componente} & \thead{Modelo}         & \thead{Especificaciones}                          \\
		\hline
		\rowcolor{white}
		CPU                 & NVIDIA Carmel (ARMv8.2) & 8 núcleos, arquitectura aarch64, 8 MB L2 + 4 MB L3 \\
		\hline
		\rowcolor{white}
		GPU                 & NVIDIA Volta            & 512 núcleos CUDA + 64 núcleos Tensor               \\
		\hline
		\rowcolor{white}
		Memoria RAM         & LPDDR5                  & 64 GB (compartida CPU/GPU)                         \\
		\hline
	\end{tabularx}
	\caption{Especificaciones técnicas del Equipo 2}
	\label{tab:equipo2_specs}
\end{table}

La arquitectura del AGX Orin presenta características diferenciales respecto a
una estación de trabajo convencional. Opera sobre arquitectura ARM. Como elemento distintivo, 
destaca su modelo de memoria unificada. Los 64 GB de RAM son compartidos entre CPU y GPU, eliminando la
necesidad de transferencias explícitas entre memorias y reduciendo la sobrecarga
asociada al movimiento de datos. No obstante, esta memoria opera a anchos de
banda inferiores a los de una GPU discreta de escritorio.

En el contexto del análisis, este equipo representa un escenario de despliegue en entornos
industriales o de robótica avanzada, donde se requiere capacidad de cómputo significativa en
un formato compacto y con consumo energético contenido. Ejemplos incluyen sistemas de inspección
visual en líneas de producción, vehículos autónomos, robótica móvil y plataformas de
adquisición de datos en campo.

\newpage

\subsubsection{Equipo 3. Sistema embebido de gama media} \label{subsubsec:equipo3}

El Equipo 3 se corresponde con una plataforma NVIDIA Jetson Orin Nano, un módulo
de sistema orientado a aplicaciones de inferencia y procesado de señales con
restricciones más severas de coste y consumo energético. Sus especificaciones se
presentan en la \autoref{tab:equipo3_specs}.

\begin{table}[h!]
	\centering
	\renewcommand{\arraystretch}{1.25}
	\setlength{\tabcolsep}{10pt}
	\begin{tabularx}{0.85\linewidth}{|>{\hsize=0.55\hsize\raggedright\arraybackslash}X|>{\hsize=0.85\hsize\centering\arraybackslash}X|>{\hsize=0.6\hsize\centering\arraybackslash}X|}
		\hline
		\rowcolor[gray]{0.90}
		\thead{Componente} & \thead{Modelo}      & \thead{Especificaciones}             \\
		\hline
		\rowcolor{white}
		CPU                 & ARMv8 (Cortex-A78AE) & 6 núcleos, arquitectura aarch64       \\
		\hline
		\rowcolor{white}
		GPU                 & NVIDIA Ampere        & 1024 núcleos CUDA + 32 núcleos Tensor \\
		\hline
		\rowcolor{white}
		Memoria RAM         & LPDDR5               & 8 GB (compartida CPU/GPU)             \\
		\hline
	\end{tabularx}
	\caption{Especificaciones técnicas del Equipo 3}
	\label{tab:equipo3_specs}
\end{table}

El Orin Nano incorpora una GPU basada en arquitectura NVIDIA Ampere con 1024
núcleos CUDA, el doble que el AGX Orin, si bien con un ancho de banda de memoria
y un consumo energético significativamente menores. Los 8 GB de memoria
compartida son un factor bastante limitante para el procesado de imágenes de gran
resolución. Al igual que el AGX Orin, opera sobre arquitectura ARM.

En el contexto de este análisis, este equipo representa un escenario de despliegue
en dispositivos de bajo coste y consumo mínimo, orientados a aplicaciones de visión
integradas en productos comerciales, sensores inteligentes, cámaras de inspección
portátiles y sistemas \textit{edge} en arquitecturas de \textit{edge computing}.

\newpage

\subsection{Análisis de resultados} \label{subsec:9_benchmarking_analysis}

En esta subsección se presentan las comparativas realizadas. 
Se analiza el impacto del dispositivo de cómputo, 
la resolución de la imagen de entrada, el tipo de dato empleado durante el 
procesado, la complejidad del \textit{pipeline}, las variantes del módulo de 
\textit{demosaicing} y las diferencias observadas entre los equipos evaluados.

\subsubsection{CPU vs GPU} \label{subsubsec:cpu_gpu_comparison}

Esta primera comparativa evalúa la ganancia de rendimiento obtenida al ejecutar los
\textit{pipelines} en GPU frente a CPU. Se toman como referencia los resultados del Equipo 1.
Para aislar el efecto del dispositivo de cómputo y evitar solapamientos con los apartados
posteriores, en este subapartado se emplean únicamente los resultados en \texttt{float32} de los perfiles
\texttt{standard\_run} y \texttt{complex\_run}.

La \autoref{tab:cpu_gpu_equipo1_float32_latency} recoge la latencia mediana\footnotemark[4]
observada en ambas plataformas para cada resolución evaluada, junto con el \textit{speedup}\footnotemark[5]
CPU/GPU.

\input{sections/9/generated/cpu_gpu_equipo1/tables/tab_cpu_gpu_float32_latency.tex}

Los resultados muestran una clara tendencia. El rendimiento en GPU aumenta al crecer
la resolución de entrada y también al incrementar la carga computacional del \textit{pipeline}.
En \texttt{standard\_run}, el \textit{speedup} pasa de \(2.44\times\) en
\texttt{256x256} a \(56.17\times\) en \texttt{2048x2048}. En
\texttt{complex\_run}, esta diferencia es mucho mayor, alcanzando
\(97.01\times\) en \texttt{2048x2048}. Este comportamiento refleja un mayor aprovechamiento de la
capacidad de paralelismo efectivo en GPU cuando aumenta el volumen de datos y el número
de operaciones por píxel, como se adelantó en la \autoref{subsec:3_8_computacion_acelerada_gpu}.

\footnotetext[4]{Se emplea la mediana en lugar de la media como métrica de latencia, ya que es menos
sensible a la presencia de valores atípicos.}

\footnotetext[5]{El \textit{speedup} se calcula como el cociente entre la latencia en CPU y la latencia en GPU.
Representa la mejora relativa en rendimiento al ejecutar en GPU.}

Complementando lo anterior, la \autoref{fig:cpu_gpu_equipo1_latency_float32} muestra la evolución de la latencia 
mediana en ambos dispositivos a medida que aumenta la resolución de la imagen de entrada. Dicha evolución se 
representa en escala logarítmica para facilitar la comparación de ambas tendencias.

\newpage

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.95\textwidth]{sections/9/generated/cpu_gpu_equipo1/figures/fig_cpu_gpu_latency_equipo1_float32.pdf}
	\caption{Latencia mediana CPU vs GPU. Eje vertical en escala logarítmica}
	\label{fig:cpu_gpu_equipo1_latency_float32}
\end{figure}

Se aprecia con claridad que ambas curvas crecen con la resolución, pero con pendientes distintas,
siendo la de CPU significativamente más pronunciada. Además, la separación entre ambas no es constante, 
sino que aumenta progresivamente con la resolución. Esta tendencia es más acusada en el \textit{pipeline} complejo. 
Para una misma resolución, la distancia entre CPU y GPU es mayor que en el \textit{pipeline} estándar, especialmente en
las resoluciones más altas. En resoluciones bajas, sobre todo en \texttt{256x256}, la ventaja de la GPU es menor, lo 
que sugiere que parte del coste total puede estar asociado a sobrecargas fijas de ejecución, transferencia o inicialización.

En la \autoref{fig:cpu_gpu_equipo1_speedup_float32} se muestra la evolución del \textit{speedup} CPU/GPU con la resolución, 
en forma de gráfico de barras para cada \textit{pipeline}.

\begin{figure}[h!]
	\centering
	\includegraphics[width=0.95\textwidth]{sections/9/generated/cpu_gpu_equipo1/figures/fig_cpu_gpu_speedup_equipo1_float32.pdf}
	\caption{\textit{Speedup} CPU/GPU por resolución}
	\label{fig:cpu_gpu_equipo1_speedup_float32}
\end{figure}

\newpage

Dicha representación confirma la tendencia evidenciada en las diferencias de latencia, mostrando la ventaja 
creciente de la GPU frente a la CPU a medida que se incrementa la resolución de la imagen de entrada.
También se observa que, para todas las resoluciones evaluadas, la ganancia relativa es mayor en el 
\textit{pipeline} complejo, lo que refuerza la conclusión de que la GPU aprovecha mejor su capacidad 
de paralelismo efectivo a medida que aumenta la carga computacional.

Desde la perspectiva de uso realista, la \autoref{tab:cpu_gpu_equipo1_realtime_30fps} 
evalúa el cumplimiento de un umbral de \(30\,\mathrm{FPS}\)\footnotemark[6]. 
En CPU, dicho objetivo deja de cumplirse para \texttt{standard\_run} en \texttt{2048x2048} 
y para \texttt{complex\_run} a partir de \texttt{1024x1024}. En GPU, en cambio, se mantiene 
un margen holgado en todas las configuraciones analizadas.

\input{sections/9/generated/cpu_gpu_equipo1/tables/tab_cpu_gpu_realtime_30fps.tex}

Los datos del Equipo 1 establecen una conclusión de vital importancia para el resto del análisis.
Y es que la elección del dispositivo de cómputo es el factor con mayor impacto sobre el rendimiento global
del sistema. La ejecución en GPU no sólo reduce la latencia absoluta, sino que también mejora la escalabilidad del 
procesado cuando aumentan la resolución y la complejidad del \textit{pipeline}. Esto implica que, para configuraciones
exigentes, el uso de CPU puede convertirse en un importante cuello de botella. 

Por tanto, la GPU se posiciona como la opción preferente para los perfiles de ejecución orientados a procesamiento en 
tiempo real. A partir de esta observación, los apartados posteriores se centran principalmente en caracterizar qué otros factores
influyen en el rednimiento una vez fijada la GPU como plataforma de referencia.

\footnotetext[6]{Una tasa de \(30\,\mathrm{FPS}\) equivale a un tiempo máximo de procesamiento de \(33.33\,\mathrm{ms}\) por imagen, 
valor habitualmente utilizado como referencia en aplicaciones de visión en tiempo real. 
Se calcula como \(FPS = \frac{1000\,\mathrm{ms/s}}{\text{latencia en ms}}\).}

\newpage

\subsubsection{Impacto de la resolución de imagen} \label{subsubsec:resolution_impact}

Este apartado analiza de forma específica cómo varía el coste temporal del procesado al
incrementar la resolución de la imagen de entrada. El objetivo es cuantificar la
sensibilidad de la latencia frente al tamaño de imagen y determinar su impacto en la
viabilidad de ejecución en tiempo real. Para mantener la comparabilidad con la
subsección anterior y evitar solapamientos con el análisis de tipo numérico, se emplean
como referencia principal los resultados en \texttt{float32} de
\texttt{standard\_run} y \texttt{complex\_run} sobre el Equipo 1.

La \autoref{tab:resolution_latency_fps_equipo1_float32} resume la latencia mediana y el
rendimiento equivalente en FPS para cada resolución y dispositivo. Se observa que la
latencia aumenta en ambos casos al crecer el tamaño de entrada, pero con una pendiente
notablemente distinta entre CPU y GPU.

\input{sections/9/generated/resolution_equipo1/tables/tab_resolution_latency_fps_equipo1_float32.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{sections/9/generated/resolution_equipo1/figures/fig_resolution_latency_equipo1_float32.pdf}
    \caption{Impacto de la resolución sobre la latencia mediana en el Equipo 1 (\texttt{float32}). Eje vertical en escala logarítmica.}
    \label{fig:resolution_latency_equipo1_float32}
\end{figure}

Tal como muestra la \autoref{fig:resolution_latency_equipo1_float32}, la separación
entre CPU y GPU se amplifica progresivamente en resoluciones altas. En
\texttt{standard\_run}, la latencia en CPU pasa de \(0.378\,\mathrm{ms}\) en
\texttt{256x256} a \(88.310\,\mathrm{ms}\) en \texttt{2048x2048}, mientras que en GPU
evoluciona de \(0.155\,\mathrm{ms}\) a \(1.572\,\mathrm{ms}\). En
\texttt{complex\_run}, la misma tendencia se mantiene, alcanzando
\(222.534\,\mathrm{ms}\) en CPU frente a \(2.294\,\mathrm{ms}\) en GPU para
\texttt{2048x2048}.

Para cuantificar la intensidad del crecimiento, la
\autoref{tab:resolution_scaling_summary_equipo1_float32} presenta el factor de escalado
por salto de resolución y el factor acumulado entre \texttt{256x256} y
\texttt{2048x2048}. Dado que este intervalo multiplica por 64 el número de píxeles,
los factores observados permiten caracterizar el comportamiento efectivo de cada
plataforma bajo incremento de carga.

\input{sections/9/generated/resolution_equipo1/tables/tab_resolution_scaling_summary_equipo1_float32.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{sections/9/generated/resolution_equipo1/figures/fig_resolution_scaling_factor_equipo1_float32.pdf}
    \caption{Factor de crecimiento de latencia por salto de resolución en el Equipo 1 (\texttt{float32}).}
    \label{fig:resolution_scaling_factor_equipo1_float32}
\end{figure}

En términos agregados, el incremento \texttt{256->2048} alcanza \(233.89\times\) y
\(132.50\times\) en CPU para los perfiles estándar y complejo, respectivamente,
mientras que en GPU se limita a \(10.17\times\) y \(6.53\times\). Por tanto,
el aumento de resolución penaliza de manera mucho más acusada la ejecución en CPU,
especialmente cuando se trabaja en el régimen de mayor tamaño de imagen.

Desde la perspectiva de despliegue, la
\autoref{fig:resolution_fps_equipo1_float32} muestra que la pérdida de FPS con la
resolución es sustancialmente más rápida en CPU. Para \texttt{complex\_run}, el umbral
de \(30\,\mathrm{FPS}\) deja de cumplirse ya en \texttt{1024x1024}, mientras que en GPU
se mantiene un margen elevado en todo el rango de resolución estudiado.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{sections/9/generated/resolution_equipo1/figures/fig_resolution_fps_equipo1_float32.pdf}
    \caption{Impacto de la resolución sobre el rendimiento en FPS en el Equipo 1 (\texttt{float32}).}
    \label{fig:resolution_fps_equipo1_float32}
\end{figure}

Como comprobación complementaria, la
\autoref{tab:resolution_scaling_by_dtype_equipo1} muestra el factor total
\texttt{256->2048} para \texttt{float16}, \texttt{float32} y \texttt{float64}. Aunque
el valor exacto depende del tipo numérico, la pauta estructural se mantiene: el impacto
de la resolución es sistemáticamente más contenido en GPU que en CPU.

\input{sections/9/generated/resolution_equipo1/tables/tab_resolution_scaling_by_dtype_equipo1.tex}

En conjunto, el análisis confirma que la resolución de entrada es una variable crítica
en el coste temporal del sistema. No obstante, su efecto está fuertemente mediado por el
dispositivo de ejecución: a medida que aumenta el tamaño de imagen, la GPU conserva una
escalabilidad significativamente superior, lo que resulta determinante para sostener
escenarios de procesado en tiempo real.

\newpage

\subsubsection{Impacto del tipo numérico} \label{subsubsec:data_type_impact}

Tras analizar el efecto del dispositivo y de la resolución, en este subapartado se
estudia la influencia del tipo numérico sobre el rendimiento del sistema. En
concreto, se comparan \texttt{float16}, \texttt{float32} y \texttt{float64} para los
perfiles \texttt{standard\_run} y \texttt{complex\_run}, utilizando los resultados del
Equipo 1 en CPU y GPU.

Con el fin de aislar este factor y observar su impacto en el régimen de mayor carga,
el análisis principal se centra en \texttt{2048x2048}. La
\autoref{tab:dtype_latency_fps_equipo1_2048} recoge la latencia mediana y el
rendimiento equivalente en FPS para cada combinación.

\input{sections/9/generated/dtype_equipo1/tables/tab_dtype_latency_fps_equipo1_2048.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{sections/9/generated/dtype_equipo1/figures/fig_dtype_latency_equipo1_2048.pdf}
    \caption{Impacto del tipo numérico en la latencia para resolución \texttt{2048x2048} (Equipo 1).}
    \label{fig:dtype_latency_equipo1_2048}
\end{figure}

Los resultados muestran dos comportamientos claramente diferenciados según el
dispositivo. En CPU, \texttt{float32} es consistentemente la opción más eficiente,
mientras que \texttt{float16} y \texttt{float64} introducen una penalización temporal
relevante. En GPU ocurre lo contrario respecto a \texttt{float16}, que presenta la menor
latencia en ambos \textit{pipelines}. Por su parte, \texttt{float64} incrementa el coste
de forma muy acusada, especialmente en GPU para alta resolución.

Para facilitar una comparación relativa independiente de la magnitud absoluta de
latencia, la \autoref{tab:dtype_ratio_vs_float32_equipo1_2048} normaliza los tiempos
respecto a \texttt{float32}. En esta representación, valores por debajo de 1 indican
mejora frente a la referencia y valores por encima de 1 indican degradación.

\input{sections/9/generated/dtype_equipo1/tables/tab_dtype_ratio_vs_float32_equipo1_2048.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{sections/9/generated/dtype_equipo1/figures/fig_dtype_ratio_vs_float32_equipo1_2048.pdf}
    \caption{Latencia normalizada respecto a \texttt{float32} para resolución \texttt{2048x2048} (Equipo 1).}
    \label{fig:dtype_ratio_vs_float32_equipo1_2048}
\end{figure}

La diferencia entre dispositivos es especialmente visible en \texttt{float64}: en
\texttt{standard\_run} su coste alcanza \(13.36\times\) el de \texttt{float32} en GPU,
frente a \(2.53\times\) en CPU. En \texttt{complex\_run}, esta relación se mantiene en
\(10.24\times\) para GPU y \(2.23\times\) para CPU. En sentido opuesto,
\texttt{float16} mejora frente a \texttt{float32} en GPU
(\(0.64\times\) y \(0.71\times\)), mientras que en CPU supone una penalización
significativa (entre \(2.30\times\) y \(4.33\times\)).

Desde la perspectiva de tiempo real, la
\autoref{fig:dtype_fps_equipo1_2048} confirma que la elección de \textit{dtype} afecta
de forma directa al margen operativo. En GPU, todos los tipos mantienen holgura sobre
\(30\,\mathrm{FPS}\), aunque \texttt{float64} reduce notablemente el margen. En CPU,
ninguna combinación en \texttt{2048x2048} alcanza dicho umbral, independientemente del
tipo numérico.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{sections/9/generated/dtype_equipo1/figures/fig_dtype_fps_equipo1_2048.pdf}
    \caption{Impacto del tipo numérico sobre el rendimiento en FPS para \texttt{2048x2048} (Equipo 1).}
    \label{fig:dtype_fps_equipo1_2048}
\end{figure}

Como comprobación de consistencia, la
\autoref{tab:dtype_consistency_1024_2048_equipo1} compara los ratios normalizados en
\texttt{1024x1024} y \texttt{2048x2048}. Aunque varía la intensidad exacta del efecto,
se conserva la pauta principal: \texttt{float32} es la referencia más equilibrada en CPU,
\texttt{float16} ofrece ventaja sistemática en GPU y \texttt{float64} introduce la mayor
penalización en ambos dispositivos.

\input{sections/9/generated/dtype_equipo1/tables/tab_dtype_consistency_1024_2048_equipo1.tex}

En conjunto, el análisis evidencia que el tipo numérico no es una decisión secundaria:
condiciona de forma significativa la latencia y la viabilidad temporal del procesado.
Para este entorno experimental, la configuración preferente para rendimiento es
\texttt{float32} en CPU y \texttt{float16} en GPU, reservando \texttt{float64} para
escenarios donde la precisión adicional justifique explícitamente su sobrecoste.

\newpage

\subsubsection{Complejidad del \textit{pipeline}} \label{subsubsec:complexity_impact}

En este subapartado se aísla la complejidad del \textit{pipeline} como factor de
análisis principal. Para ello se comparan de forma directa los perfiles
\texttt{standard\_run} y \texttt{complex\_run} sobre el Equipo 1, manteniendo
\texttt{float32} como tipo numérico fijo con el fin de evitar interferencias de otros
factores ya estudiados en apartados anteriores.

La métrica central es el ratio \(\textit{complex}/\textit{standard}\), que cuantifica
el sobrecoste relativo asociado al incremento de operaciones del pipeline.
La \autoref{tab:complexity_ratio_equipo1_float32} resume este ratio por resolución y
dispositivo.

\input{sections/9/generated/complexity_equipo1/tables/tab_complexity_ratio_equipo1_float32.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{sections/9/generated/complexity_equipo1/figures/fig_complexity_ratio_equipo1_float32.pdf}
    \caption{Sobrecoste relativo introducido por la complejidad del pipeline en \texttt{float32} (Equipo 1).}
    \label{fig:complexity_ratio_equipo1_float32}
\end{figure}

Se observa que el impacto relativo de la complejidad es sistemáticamente mayor en CPU
que en GPU para todas las resoluciones evaluadas. En CPU, el ratio oscila entre
\(2.52\times\) y \(5.08\times\), mientras que en GPU se sitúa entre
\(1.46\times\) y \(2.27\times\). Esta diferencia sugiere que la GPU absorbe con mayor
eficiencia el aumento de carga computacional asociado al \textit{pipeline} complejo.

El resumen agregado de la \autoref{tab:complexity_summary_equipo1_float32} confirma esta
tendencia: el ratio medio es \(3.87\times\) en CPU frente a \(1.84\times\) en GPU,
con una mediana de \(3.94\times\) y \(1.81\times\), respectivamente.

\input{sections/9/generated/complexity_equipo1/tables/tab_complexity_summary_equipo1_float32.tex}

Desde la perspectiva absoluta, la
\autoref{tab:complexity_delta_latency_fps_equipo1_float32} y la
\autoref{fig:complexity_delta_latency_equipo1_float32} muestran que la complejidad
incrementa la latencia en ambos dispositivos, aunque con distinta magnitud práctica.
En \texttt{2048x2048}, el incremento en CPU es de
\(134.224\,\mathrm{ms}\), frente a \(0.722\,\mathrm{ms}\) en GPU.

\input{sections/9/generated/complexity_equipo1/tables/tab_complexity_delta_latency_fps_equipo1_float32.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{sections/9/generated/complexity_equipo1/figures/fig_complexity_delta_latency_equipo1_float32.pdf}
    \caption{Incremento absoluto de latencia al pasar de \texttt{standard\_run} a \texttt{complex\_run} en \texttt{float32} (Equipo 1).}
    \label{fig:complexity_delta_latency_equipo1_float32}
\end{figure}

En términos operativos, la complejidad también reduce de forma notable el margen de
tiempo real, especialmente en CPU. La
\autoref{fig:complexity_fps_drop_equipo1_float32} refleja caídas de rendimiento entre
el \(60.3\%\) y el \(80.3\%\) en CPU, frente a descensos entre el \(31.5\%\) y el
\(56.0\%\) en GPU.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{sections/9/generated/complexity_equipo1/figures/fig_complexity_fps_drop_equipo1_float32.pdf}
    \caption{Caída porcentual de FPS al aumentar la complejidad del pipeline en \texttt{float32} (Equipo 1).}
    \label{fig:complexity_fps_drop_equipo1_float32}
\end{figure}

En conjunto, el análisis confirma que la complejidad del \textit{pipeline} constituye un
factor de primer orden en el rendimiento del sistema. Bajo las condiciones evaluadas,
el aumento de complejidad penaliza de forma acusada la ejecución en CPU y de forma
moderada en GPU, por lo que la elección del dispositivo resulta crítica para preservar
la viabilidad temporal cuando se incorporan etapas de procesado adicionales.

\newpage

\subsubsection{Comparativa de módulos de \textit{Debayer}} \label{subsubsec:debayer_comparison}

En este subapartado se compara el rendimiento de los módulos de \textit{Debayer}
implementados, aislando esta operación como factor principal de análisis. Dado que el
\textit{debayering} constituye una etapa computacionalmente intensiva y que, en
escenarios de despliegue reales, este tipo de procesado se ejecuta típicamente en GPU,
el estudio se centra en los resultados de GPU del Equipo 1. Para evitar interferencias
del tipo numérico, se fija \texttt{float32} en toda la comparativa.

La \autoref{tab:debayer_gpu_latency_equipo1_float32} muestra la latencia mediana de cada
módulo para las distintas resoluciones evaluadas.

\input{sections/9/generated/debayer_equipo1/tables/tab_debayer_gpu_latency_equipo1_float32.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{sections/9/generated/debayer_equipo1/figures/fig_debayer_gpu_latency_vs_resolution_equipo1_float32.pdf}
    \caption{Comparativa de latencia de módulos de Debayer en GPU para \texttt{float32} (Equipo 1).}
    \label{fig:debayer_gpu_latency_vs_resolution_equipo1_float32}
\end{figure}

Se observa un patrón de ordenación estable en todo el rango de resoluciones: la variante
\texttt{Debayer2x2} presenta sistemáticamente la menor latencia, seguida de
\texttt{DebayerSplit}, \texttt{Debayer3x3} y \texttt{Debayer5x5}. Esta jerarquía se
mantiene desde \texttt{256x256} hasta \texttt{2048x2048}, lo que sugiere un
comportamiento consistente de las implementaciones en el dispositivo de referencia.

Para cuantificar la diferencia en el caso más exigente, la
\autoref{tab:debayer_gpu_ranking_2048_equipo1_float32} presenta el ranking en
\texttt{2048x2048}. La latencia de \texttt{Debayer5x5} es \(1.56\times\) la del mejor
caso, mientras que \texttt{DebayerSplit} se mantiene en un margen contenido
\((1.20\times)\).

\input{sections/9/generated/debayer_equipo1/tables/tab_debayer_gpu_ranking_2048_equipo1_float32.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.86\textwidth]{sections/9/generated/debayer_equipo1/figures/fig_debayer_gpu_ranking_2048_equipo1_float32.pdf}
    \caption{Ranking de módulos de Debayer en \texttt{2048x2048} (GPU, \texttt{float32}, Equipo 1).}
    \label{fig:debayer_gpu_ranking_2048_equipo1_float32}
\end{figure}

La comparación relativa normalizada al mejor módulo (valor \(1.00\times\)) se recoge
en la \autoref{tab:debayer_gpu_relative_vs_best_equipo1_float32} y en la
\autoref{fig:debayer_gpu_relative_vs_best_equipo1_float32}. En resoluciones bajas, la
distancia entre variantes es mayor (hasta \(1.82\times\) en \texttt{256x256}), mientras
que en resoluciones altas se reduce parcialmente para algunas implementaciones
intermedias, sin alterar el orden relativo global.

\input{sections/9/generated/debayer_equipo1/tables/tab_debayer_gpu_relative_vs_best_equipo1_float32.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.90\textwidth]{sections/9/generated/debayer_equipo1/figures/fig_debayer_gpu_relative_vs_best_equipo1_float32.pdf}
    \caption{Comparativa relativa de módulos Debayer respecto al mejor caso (GPU, \texttt{float32}, Equipo 1).}
    \label{fig:debayer_gpu_relative_vs_best_equipo1_float32}
\end{figure}

Desde la perspectiva operativa, incluso en \texttt{2048x2048} todos los módulos
mantienen tasas elevadas de procesamiento en GPU (por encima de \(700\,\mathrm{FPS}\)
en todos los casos). No obstante, cuando el presupuesto temporal del pipeline global es
estricto, la elección del módulo de \textit{Debayer} sigue siendo relevante, ya que el
sobrecoste relativo entre variantes puede condicionar el margen disponible para etapas
posteriores de procesado.

En conjunto, el análisis confirma que \texttt{Debayer2x2} constituye la opción más
eficiente en términos de latencia para el entorno evaluado, mientras que
\texttt{DebayerSplit} se posiciona como alternativa intermedia con coste moderado y
\texttt{Debayer3x3}/\texttt{Debayer5x5} implican una penalización superior.

\newpage

\subsubsection{Comparativa entre plataformas y validación en escenarios de uso realista en tiempo real} \label{subsubsec:platform_comparison_results}

Los resultados analizados en los subapartados anteriores han permitido caracterizar con
detalle el comportamiento del sistema bajo una plataforma de muy altas prestaciones
(Equipo 1), útil como referencia de techo de rendimiento. Sin embargo, en escenarios de
despliegue industrial y \textit{edge}, la ejecución suele realizarse sobre sistemas
embebidos con restricciones más estrictas de potencia, memoria y capacidad de cómputo.
Por ello, en este apartado se amplía la comparación a los Equipos 2 y 3, con el objetivo
de evaluar la validez operativa del sistema en condiciones más próximas a producción
real.

Para mantener la comparabilidad con el resto del estudio, se utilizan los perfiles
\texttt{standard\_run} y \texttt{complex\_run} en \texttt{float32}, considerando CPU y
GPU para todas las resoluciones evaluadas. La
\autoref{tab:interteam_latency_fps_float32} resume la latencia mediana y el rendimiento
equivalente en FPS para el conjunto completo de configuraciones.

\input{sections/9/generated/interteam/tables/tab_interteam_latency_fps_float32.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{sections/9/generated/interteam/figures/fig_interteam_latency_standard_float32.pdf}
    \caption{Comparativa de latencia entre equipos para \texttt{standard\_run} en \texttt{float32}.}
    \label{fig:interteam_latency_standard_float32}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{sections/9/generated/interteam/figures/fig_interteam_latency_complex_float32.pdf}
    \caption{Comparativa de latencia entre equipos para \texttt{complex\_run} en \texttt{float32}.}
    \label{fig:interteam_latency_complex_float32}
\end{figure}

En términos generales, la jerarquía de rendimiento se mantiene en todas las condiciones
analizadas: Equipo 1 (referencia de escritorio) presenta las menores latencias,
seguido del Equipo 2 y, finalmente, del Equipo 3. Esta tendencia es especialmente
visible en GPU para altas resoluciones y \textit{pipelines} complejos. Aun así, el Equipo
2 conserva un comportamiento competitivo para despliegue, manteniendo márgenes de
ejecución relevantes en tareas de visión en tiempo real.

Desde la perspectiva de viabilidad operativa, la
\autoref{tab:interteam_realtime_thresholds_30fps} recoge la máxima resolución que
mantiene al menos \(30\,\mathrm{FPS}\) por combinación de equipo, pipeline y
dispositivo. Este criterio permite traducir los resultados de latencia a una métrica
directamente interpretable en escenarios de producción.

\input{sections/9/generated/interteam/tables/tab_interteam_realtime_thresholds_30fps.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{sections/9/generated/interteam/figures/fig_interteam_fps_realtime_map_float32.pdf}
    \caption{Mapa de cumplimiento del umbral de \(30\,\mathrm{FPS}\) para \texttt{float32} en los tres equipos.}
    \label{fig:interteam_fps_realtime_map_float32}
\end{figure}

Los resultados muestran que el Equipo 2 en GPU cumple el umbral de
\(30\,\mathrm{FPS}\) incluso en \texttt{2048x2048} para ambos perfiles
(\(\sim59.0\,\mathrm{FPS}\) en \texttt{standard\_run} y
\(\sim31.8\,\mathrm{FPS}\) en \texttt{complex\_run}). En el Equipo 3, la GPU mantiene
\texttt{2048x2048} en el perfil estándar
(\(\sim32.1\,\mathrm{FPS}\)), pero no en el complejo
(\(\sim17.4\,\mathrm{FPS}\)), donde la frontera práctica se sitúa en
\texttt{1024x1024}. En CPU embebida (Equipos 2 y 3), la viabilidad decrece con rapidez
al aumentar resolución, especialmente en \texttt{complex\_run}.

Para dimensionar la pérdida relativa respecto al entorno de referencia, la
\autoref{tab:interteam_normalized_vs_equipo1_gpu} y la
\autoref{fig:interteam_gpu_scaling_float32} presentan una comparación normalizada en GPU
tomando Equipo 1 como base. Se observa que el impacto no es uniforme: el Equipo 2
mantiene una degradación moderada frente al Equipo 1, mientras que el Equipo 3 muestra
una separación mayor, particularmente en el perfil complejo.

\input{sections/9/generated/interteam/tables/tab_interteam_normalized_vs_equipo1_gpu.tex}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{sections/9/generated/interteam/figures/fig_interteam_gpu_scaling_float32.pdf}
    \caption{Comparativa de escalado en GPU entre equipos para \texttt{float32} (normalización en \texttt{256x256}).}
    \label{fig:interteam_gpu_scaling_float32}
\end{figure}

En conjunto, este análisis confirma que los resultados obtenidos en Equipo 1 son útiles
como referencia de capacidad máxima, pero no suficientes para validar despliegue realista
por sí solos. Para escenarios de producción con \textit{pipelines} complejos y resolución
alta, el perfil representado por el Equipo 2 constituye la frontera de viabilidad robusta
en tiempo real dentro de las plataformas embebidas evaluadas. El Equipo 3 resulta
adecuado para cargas estándar y resoluciones altas, o para cargas complejas con
resolución moderada.
